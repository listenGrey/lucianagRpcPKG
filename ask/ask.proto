syntax = "proto3";

option go_package = "ask/";

package lucianagRpcPKG;

message ChatContext {
  string Topic = 1;
  repeated string Prerequisites = 2;
  string Request = 3;
}

message Result {
  string Response = 1;
}

service ResponseService {
  rpc Response(ChatContext) returns (Result) {}
}